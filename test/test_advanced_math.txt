from "lib" import print_float print_int;


section .text:

entry {
    $vari: int.32;
    $varf: float.64;
    //$vard: float.64;

    $vari <- 100;
    $vari /= 10;
    $vari *= 10;

    call [callconv] print_int($vari);

    $varf <- 100.35;
    $varf /= 10.0;
    $varf *= 10.0;

    //#cvtss2sd $vard, $varf;

    call [callconv] print_float($varf);
}

