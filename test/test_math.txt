from "c" import puts;
from "lib" import print_int;


section .text:

fn [callconv] add($a: int.16, $b: int.16) -> int.16 {
    result <- 0;
    result += $a;
    result += $b;
}


entry {
    $value: int.32;

    $value <- 15;
    call add($value, 10) -> $value;

    call [callconv] print_int($value);
}

